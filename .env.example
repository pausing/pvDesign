# Public site origin, no trailing slash. Optional when the UI and API share a host.
CORS_ORIGINS=https://your-domain.com

# Used by Nixpacks / Docker. Dokploy: mount a volume at /data.
STATIC_DIR=frontend/dist
PVDES_DATA_DIR=/data
